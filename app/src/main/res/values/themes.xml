<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ScreenRecording" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.ScreenRecording" parent="Base.Theme.ScreenRecording"></style>

    <style name="Theme.ScreenRecording.DialogAnimation" parent="Theme.ScreenRecording">
        <item name="android:windowEnterAnimation">@anim/slide_in_bottom</item>
        <item name="android:windowExitAnimation">@anim/slide_out_bottom</item>
    </style>
</resources>